{
  "name": "iness-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && node deploy.cjs"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ftp-deploy": "^2.4.7",
    "three": "^0.178.0"
  }
}
